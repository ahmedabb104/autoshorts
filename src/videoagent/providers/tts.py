"""`TTSProvider` protocol and its implementations.

ElevenLabs for quality, a local Piper provider as the zero-cost dev default. Each
implementation reports its spend so the run's cost accumulator stays accurate.

Populated in Phase 2.
"""
