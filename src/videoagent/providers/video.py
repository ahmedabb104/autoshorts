"""`VideoProvider` protocol and its implementations.

Default is stock assembly: search Pexels/Pixabay for clips matching the script beats and
download them. A generative implementation (fal.ai / Veo / similar) is an alternate
implementation, off by default.

Populated in Phase 2.
"""
